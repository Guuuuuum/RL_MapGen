#pragma once

#include "level_generation.h"

class Tower
{
private:
    Map& map;
public:
    Tower(Map& in_map) : map(in_map) {}

    void generate(Room in_room)
    {
        
    }
};
